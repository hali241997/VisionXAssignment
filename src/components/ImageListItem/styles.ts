import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    backgroundColor: "white",
  },
  image: {
    height: 200,
  },
  contentContainer: {
    padding: 10,
  },
});
